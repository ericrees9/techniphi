import React from 'react';
import '../MovieCard/MovieCard.css';

function MovieCard () {
    return(
        <div className="cardBox">
            <h2>This will be the movie card.</h2>
        </div>
    );
}

export default MovieCard;